Centraliser les configs toujours accessible en mémoire
Deep symbolize config

Ajouter des options à issues au runner
0  -a et --assignee
    String User login
    none for Issues with no assigned User.
    * for Issues with any assigned User.
1/2  -c et --creator
    String User login.
1/2  -d et --direction
    asc or desc, default: desc.
1/2  -l et --labels
    String list of comma separated Label names. Example: bug,ui,@high
0  -m et --milestone
    Integer Milestone number
    none for Issues with no Milestone.
    * for Issues with any Milestone.
0  -M et --mentioned
    String User login.
0  -s et --state
    open, closed, default: open
1/2  -S et --sort
    created, updated, comments, default: created.
0  --since
    Optional string of a timestamp in ISO 8601 format: YYYY-MM-DDTHH:MM:SSZ


Ajouter des fixtures

Afficher le no et le titre des issues


Show issues (gharial issues show:#  )
  no
  title
  description
  label
  milestone
  creator
  assignee
  state
  created_at
  updated_at

  -c & --comments
  -C & --comments-only

Ajouter la gestion de la pagination

Ajouter la collection milestones
